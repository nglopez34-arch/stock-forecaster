Overview:
fetch_data.py creates and/or maintains raw_data.parquet using the alpaca api. process_data.py then turns this into processed_data.parquet, by doing all preprocessing (normalization, intrpolation, etc.) and time-series feature creation for both models (volatility forecaster and log return forecaster). Note that not all columns of this parquet will be used by both models.
 ***insert how the models are trained right here***
 The volatility model runs first, and its forecast is used as an input for the LSTM log return forecaster, among other features.

 *Note:
 You will probably have to have a .py file that will generate non-time-series features to be used for each training/inference tensors for the log return model. For example, there will probably only be a single value generated by the volatility model. Some cyclical time features (day of week, etc.) may also only have one value for each tensor.

Note added 5/16: This right here might be a good way to continously update the portfolio based on the forecasts:
https://claude.ai/share/90d1f701-f833-4f77-b424-566a39fb3ecb
