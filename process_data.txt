These are the things that I need to do in process_data.py:
Check to see if there already exists processed_data.parquet
Interpolate gaps in raw_data
Look for outliers and remove them (optional)

* = feature for volatility forecaster (TCN)
+ = feature for log return forecaster (LSTM)

Time series features:
+*minute by minute log returns
+ln(volume)
+volume surprise ((vol - ewma volume) / ewma std of volume)
+ewma of log returns (5,15,60)
+log volatility (std over 5, 15, 60 min)
+Z-scored returns over multiple horizons
+EMA return differentials (EMA_5(r) - EMA_20(r))
+*cyclical time features
+s&p 500 log returns
*squared log return r^2
*log squared return ln(r^2 + epsilon(for numerical stability))
*std of log returns over last (15, 60, 240) min
*ewma volatility (0.97)
*normalized range (H-L)/C
*normalized volume (volume / 30 min rolling average volume)
*volatility of volatility (std of rv(60) for the past 30 min)



context scalars:
+30 day realized volatility
+average daily volume (ewma)
+forecasted volatility from volatility forecasting model
